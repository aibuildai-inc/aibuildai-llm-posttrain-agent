{%- macro render() -%}
# Search

This run uses Linear Search. It starts with one design and follows it with one serial line of revisions: each finished attempt may lead to exactly one next attempt. The Aggregator then combines every finished attempt in the chain into one ensemble, and the run keeps whichever attempt scored best.
{%- endmacro %}
