{%- macro render() -%}
# Search

This run uses NB Tree Search. One Worker completes each design. A Worker builds and runs the whole design, then may give revisions to child Workers. The framework scores the design only after the Worker session ends.
{%- endmacro %}
