{%- macro render() -%}
# Search

This run uses NB Search. One Worker completes each design. A Worker builds and runs the whole design, then gives one revision to the next Worker. The framework scores the design only after the Worker session ends.
{%- endmacro %}
