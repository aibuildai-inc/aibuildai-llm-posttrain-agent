"""One run's complete state and behavior.

This package exports nothing. Every consumer, inside or outside engine, imports the concrete home module directly, so each name has exactly one import route.
"""
