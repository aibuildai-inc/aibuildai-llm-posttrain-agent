"""Built-in memory corpus + the engines that read and curate it.

The corpus contains task histories, cross-task patterns, and the scalar task_categories.json mapping. memory/store.py serves it through a BundleMemoryStore built once at launch from the corpus root.
"""
