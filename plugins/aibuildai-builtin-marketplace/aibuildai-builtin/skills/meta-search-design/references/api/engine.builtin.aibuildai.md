<!-- DO NOT EDIT — generated by scripts/gen_reference.py -->

# Table of Contents

* [engine.builtin.aibuildai](#engine.builtin.aibuildai)
  * [DeliveryRecord](#engine.builtin.aibuildai.DeliveryRecord)
  * [SearchOutput](#engine.builtin.aibuildai.SearchOutput)
  * [SearchResult](#engine.builtin.aibuildai.SearchResult)
      * [task\_data](#engine.builtin.aibuildai.task_data)

<a id="engine.builtin.aibuildai"></a>

# engine.builtin.aibuildai

The shared AIBuildAI product package.

What every AIBuildAI run has whatever algorithm it selected: the product
Search shell and its fixed lifecycle, the Definitions that lifecycle owns
(Setup, Submitter, Finalizer, the whole-run Writer and the Score Program), and
the two Output contracts the product speaks.

The names re-exported here are the ones a Search written OUTSIDE the product --
a package a MetaAgent generates -- may use: its top-level Search returns the
same ``SearchOutput`` a built-in package returns, and the product shell turns
that into the user's ``SearchResult``.

<a id="engine.builtin.aibuildai.DeliveryRecord"></a>

## DeliveryRecord Objects

```python
class DeliveryRecord(WorkflowBaseModel)
```

What one run handed the user: the delivered files and one sentence about them.

The ONE definition of the delivery shape. The Finalizer produces it and the
completed Search returns the same record, so the two boundaries cannot
drift apart and reject each other's value. Frozen like the terminal
Outputs that carry it: both may hold the same instance, so a mutable
nested record would let one journaled Output change the other with no
event behind it.

<a id="engine.builtin.aibuildai.SearchOutput"></a>

## SearchOutput Objects

```python
class SearchOutput(ScoredAttempt)
```

The scored result selected by a Search, before delivery.

<a id="engine.builtin.aibuildai.SearchResult"></a>

## SearchResult Objects

```python
class SearchResult(SuccessfulOutput)
```

The final product result handed to the user.

The Search's own Output combined with the independent delivery the
Finalizer wrote from it.

<a id="engine.builtin.aibuildai.task_data"></a>

#### task\_data

```python
def task_data() -> FileRef
```

The run's public data: what SETUP prepared, plus the real material behind every link it authored there.

The one door to the task for every solving role. It is an index as much as
a directory -- a link is only as readable as its target -- so this is where
the product says so, and the launch boundary binds what it resolves to.
