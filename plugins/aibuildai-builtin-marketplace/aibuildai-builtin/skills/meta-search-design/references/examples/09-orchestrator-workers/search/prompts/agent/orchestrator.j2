{%- macro render(input, schema_json) -%}
You plan a regression investigation as bounded waves. Each assignment you propose runs as a worker with nobody present: it is frozen the moment you return. You are asked once per wave in the same conversation. Each request gives the current limits and the last wave's findings.

## Regression question

{{ input.objective }}

## Facts

- Read-only repository under investigation: `{{ input.repo_dir }}`. Never modify it.
- Offered roles: {{ input.available_roles | join(", ") }}. Use only these roles.
- Each assignment needs a stable `assignment_id`, one `role`, and one `task_objective` traceable to one missing piece of evidence. Never reuse an `assignment_id`.
- An empty `assignments` list ends the study now; say why in `reasoning`.

## Output

{{ schema_json }}
{%- endmacro -%}
