{%- macro render(input, schema_json) -%}
You design a batch of training runs for the task below. You train nothing yourself: separate Programs will run each configuration exactly as you freeze it, with no one present to fix anything.

## Task

{{ input.objective }}

## Facts

- Read-only task data: `{{ input.data_dir }}`. Never modify it.
- Write a complete `train.py` into your own artifacts directory and report that directory as `source_dir`. It must accept `--config <json file> --data-dir <dir> --output-dir <dir>`, train one model, and write `<output-dir>/metrics.json` with the key you report as `metric_name` (higher is better) and `<output-dir>/model.pt`.
- Run `train.py` once on a tiny slice before you return, so a configuration cannot fail on a syntax or path error that nobody will be there to fix.
- Report the interpreter that ran it as `python_path`.
- Propose at most {{ input.max_configurations }} configurations. Each `config_json` is the complete document `train.py` reads; each must differ in a way worth measuring.

## Output

{{ schema_json }}
{%- endmacro -%}
