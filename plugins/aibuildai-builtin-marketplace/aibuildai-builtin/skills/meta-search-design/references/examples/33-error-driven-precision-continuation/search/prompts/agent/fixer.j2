{%- macro render(input, schema_json) -%}
You diagnose one evaluation round's failures for the task below and write the synthetic data that fixes only what is actually broken.

## Task

{{ input.objective }}

## Current state

The checkpoint under evaluation scored {{ input.current_score }}. Its per-example failures are at `{{ input.failures_path }}`, one JSON object per line with the keys `input`, `expected`, and `produced`. The output conventions and the eval's exact rendering chain are documented at `{{ input.conventions_path }}`.

## What to do

- Read every failed example and compare `produced` against `expected` under the documented conventions. Categorize each error into a specific, nameable convention violation (a formatting rule, a normalization rule, an argument-inclusion policy), not a vague "wrong answer".
- Report each fixable category in `fixable_errors`. A failure that is genuinely ambiguous under the documented conventions, or that no synthetic example can exercise, is not fixable: leave it out. If none of the failures are fixable this way, return an empty `fixable_errors` and an empty `fix_data_dir`.
- When at least one category is fixable, write a small synthetic training set (hundreds of examples, not a full regeneration) that specifically exercises the broken conventions, generated by template, not by re-solving the task. Mix in a replay sample: draw a `{{ input.replay_fraction }}` fraction of the fix dataset from `{{ input.replay_data_dir }}`, examples the checkpoint already gets right, so the continuation round cannot fix one convention by breaking another.
- Write the mixed dataset into your own artifacts directory and report that directory as `fix_data_dir`.

## Facts

- Read-only replay source: `{{ input.replay_data_dir }}`. Never modify it.

## Output

{{ schema_json }}
{%- endmacro -%}
