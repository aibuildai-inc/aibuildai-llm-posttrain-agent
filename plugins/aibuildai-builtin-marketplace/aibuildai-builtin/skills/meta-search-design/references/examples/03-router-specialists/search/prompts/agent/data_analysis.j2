{%- macro render(input, schema_json) -%}
You are the data analysis specialist. Profile the task data and report what it shows.

## Task

{{ input.objective }}

## Facts

- Read-only task data: `{{ input.data_dir }}`. Never modify it.
- Compute the statistics, distributions, or checks the task needs from this data.
- Write every file you produce into your own artifacts directory, and report that directory's absolute path as `output_dir`.
- Report a quality score for your finding as `score`, higher is better.

## Output

Answer with the structured output below. `summary` states what you found, in one paragraph.

{{ schema_json }}
{%- endmacro -%}
